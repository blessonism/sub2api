# brainstorm: Token 用量自动分组倍率

## Goal

基于用户最近 7 天或最近 30 天的 Token 使用量，自动给用户在指定分组下分配专属计费倍率，或同时把用户授权到目标分组，并提供一个管理员页面来配置规则、查看命中用户、执行或预览分配结果。

## What I already know

- 用户希望按近期 Token 用量自动分配某个分组的专属倍率，窗口至少包括最近 7 天和最近 30 天。
- 现有系统已经有管理员用量统计、用户消耗排行榜、用户分组统计和用量日志聚合能力。
- 现有系统已经存在 `(user_id, group_id) -> rate_multiplier` 的用户专属分组倍率表与接口。
- 管理员分组页已有手动维护“分组下用户专属倍率”的弹窗，当前能力是按分组手动添加用户并批量保存倍率。
- 管理员希望在分组倍率管理中支持“选中多个条目，输入一个统一倍率，一键应用”，而不是只能逐个调整。
- 用量日志里的 Token 统计口径多处使用 `input_tokens + output_tokens + cache_creation_tokens + cache_read_tokens`。
- 计费链路会优先使用用户在当前分组下的专属倍率；没有专属倍率时使用分组默认倍率。
- 本需求的 Token 阈值口径已确认：输入 Token + 输出 Token + 缓存创建 Token + 缓存读取 Token，并只统计成功落账请求。

## Assumptions

- 规则动作需要支持管理员配置：既可以只维护用户在目标分组下的专属倍率，也可以同时把用户授权到目标分组。
- 自动规则应由管理员配置，不能对普通用户开放。
- 最近 7 天/30 天应优先按滚动窗口计算，而不是自然周或自然月。
- 每条策略需要支持管理员配置执行频率，并支持页面上的手动预览与立即执行。
- 规则结构采用“一个策略内多个有序档位”：系统按用户 Token 用量命中的最高档位执行，避免多条独立规则互相覆盖。
- 目标分组放在策略层配置；档位层只配置 Token 阈值和倍率。选择“授权分组并设置专属倍率”时，命中用户会被授权到该策略的目标分组。
- 管理员可为策略配置“自动规则优先”或“手动设置优先”；默认使用手动设置优先。
- Token 用量统计范围由策略配置筛选条件决定；默认不筛选，即全站总用量。
- 同一目标分组同一时间只允许一条启用中的自动策略，避免多策略同时覆盖同一用户同一分组。

## Requirements

- 管理员可以创建、编辑、启停 Token 用量自动策略。
- 每条策略固定一个目标分组，同一目标分组同一时间只允许一条启用策略。
- 每条策略包含多个有序档位；档位只配置最小 Token 阈值和目标倍率。
- 执行时按用户窗口期 Token 用量选择命中的最高档位；低于最低档时清除本策略产生的配置。
- 窗口期支持 7 天和 30 天，按滚动窗口计算。
- Token 阈值口径为输入 Token + 输出 Token + 缓存创建 Token + 缓存读取 Token。
- 只统计成功落账请求，失败或未落账请求不进入阈值判断。
- 策略筛选范围支持分组、模型、请求类型、计费类型；默认不筛选，即全站总用量。
- 动作模式支持：
  - 仅设置专属倍率：只写入目标分组下的用户专属倍率。
  - 授权分组并设置专属倍率：新增/保留目标分组授权，同时写入专属倍率。
- 授权分组模式不删除用户已有旧分组；清除时只移除本策略新增的目标分组授权。
- 冲突策略支持：
  - 手动设置优先：默认值。管理员手动改过倍率或分组授权后，自动规则不再覆盖对应用户配置。
  - 自动规则优先：规则执行可按命中档位覆盖自动管理范围内的配置。
- 自动降档或清除只能处理由本策略产生的配置，不能误删管理员手动设置。
- 自动写入不能破坏现有 RPM override 或其它用户专属配置。
- 执行频率支持 every_6h、daily、weekly。
- 支持定时自动执行、手动预览、立即执行和执行历史。
- 执行历史需要支持查看用户级变更明细，包括用户、Token 用量、命中档位、变更类型、原倍率、新倍率和跳过/失败原因。
- 预览只返回将新增、更新、降档、清除、跳过的用户列表，不写入数据库。
- 同一策略不能并发执行。
- 管理员可以在手动分组倍率管理中勾选多个条目，输入同一个倍率并一次性应用到所有选中条目。
- 手动批量统一倍率的输入必须是数字、必须大于 0，且最多保留两位小数；暂不设置倍率上限。
- 手动批量统一倍率应按全部成功或全部失败处理，未选中的条目不能被修改。

## Acceptance Criteria

- [ ] 管理员能配置至少一条近 7/30 天 Token 阈值到分组专属倍率的策略。
- [ ] 同一目标分组同一时间只能启用一条自动策略。
- [ ] 管理员能选择动作模式、执行频率、冲突策略和统计筛选范围。
- [ ] 系统使用四项 Token 之和判断档位，并排除失败/未落账请求。
- [ ] 筛选范围只影响用量聚合和档位命中，不隐式改变目标分组。
- [ ] 预览不会写入任何用户配置或执行归属记录。
- [ ] 立即执行与定时执行产生一致变更和执行记录。
- [ ] 命中用户获得目标分组下的专属倍率。
- [ ] 授权分组模式会授予目标分组访问权，但不会删除旧分组。
- [ ] 用量下降后自动切换到低档倍率。
- [ ] 不再满足任何档位后，系统清除本策略产生的专属倍率/分组授权。
- [ ] 手动设置优先时，自动规则不会覆盖管理员手动接管的配置。
- [ ] 自动规则优先时，规则执行可按命中档位覆盖自动管理范围内的配置。
- [ ] 执行历史可追溯成功、失败、变更数量和错误原因。
- [ ] 管理员能在执行历史中展开查看用户级变更明细，确认哪些用户的倍率由多少调整到多少。
- [ ] 管理员能勾选多个手动分组倍率条目，输入一个大于 0 且最多两位小数的倍率，并一键统一应用。
- [ ] 手动批量统一倍率拒绝空值、非数字、0、负数和超过两位小数的输入。
- [ ] 手动批量统一倍率只更新选中条目，未选中条目保持原倍率。
- [ ] 手动批量统一倍率出现校验或写入失败时，不产生部分成功的模糊状态。

## Technical Approach

- 新增自动策略、档位、归属记录和执行历史四类数据模型。
- 新增自动执行归属记录：记录哪些用户配置由哪条策略写入、当前档位、上次写入值和人工接管状态，用于安全降档/清除。
- 复用现有用量日志聚合口径，按策略筛选范围聚合每个用户的窗口 Token。
- 复用现有用户专属分组倍率能力写入 `(user_id, group_id) -> rate_multiplier`。
- 手动批量统一倍率优先复用现有用户专属分组倍率写入链路，并在前后端同时校验倍率格式和选中范围。
- 授权分组模式复用现有用户 allowed groups 能力；自动清除时只清除该策略产生的授权。
- 新增策略服务负责校验、预览、执行、降档、清除和执行记录。
- 新增调度执行器，按 `next_run_at` 扫描到期策略，并使用策略级锁避免并发。
- 新增管理员 API 前缀 `/api/v1/admin/token-usage-policies`。
- 前端新增管理员页面 `/admin/token-usage-policies`，提供策略列表、表单、预览、立即执行、历史展示和历史明细展开。

## Decision (ADR-lite)

**Context**: 需要根据近期 Token 使用量自动维护用户在某个分组下的专属倍率，并支持分组授权、降档和管理员可控执行。

**Decision**: 采用“单策略固定目标分组 + 多有序档位”的模型；同一目标分组只允许一条启用策略；策略级配置执行动作、执行频率、统计筛选范围和冲突处理策略；默认手动设置优先。

**Consequences**: 实现稳定、降档语义清晰、管理页容易理解；暂不支持同一策略内不同档位分配不同分组，也不支持同目标分组多策略优先级。

## Implementation Plan

- PR1: 后端数据模型、迁移、策略/档位基础 CRUD 与唯一启用约束。
- PR2: 用量聚合、预览计算、自动写入、降档/清除、执行归属记录。
- PR3: 调度执行器、立即执行、执行锁、执行历史。
- PR4: 管理员页面、表单、档位编辑、筛选条件、预览与执行历史。
- PR5: 测试补齐、边界校验、文案与文档整理。

## Out of Scope

- MVP 不做迁移并移除旧分组。
- MVP 不做单策略内不同档位分配不同目标分组。
- MVP 不做同一目标分组多策略优先级或最优惠倍率合并。
- MVP 不开放自由 cron 表达式，只提供受控频率枚举。

## Definition of Done

- 后端相关单元测试和迁移/约束测试覆盖核心逻辑。
- 前端页面和表单测试覆盖主要交互。
- 后端针对性 `go test` 单次控制在 60 秒内。
- 前端针对性测试和类型检查通过。

## Technical Notes

- 本仓库是 `Wei-Shaw/sub2api` 下游二开，任务基线为 `custom/main`，分支为 `feature/token-usage-auto-group-multiplier`。
- 实现和检查上下文必须包含 `.trellis/spec/guides/downstream-fork-workflow.md`。
- 相关现有接口：
  - `GET /api/v1/admin/groups/:id/rate-multipliers`
  - `PUT /api/v1/admin/groups/:id/rate-multipliers`
  - `DELETE /api/v1/admin/groups/:id/rate-multipliers`
- 相关现有文件：
  - `backend/internal/service/user_group_rate.go`
  - `backend/internal/repository/user_group_rate_repo.go`
  - `backend/internal/repository/usage_log_repo.go`
  - `frontend/src/components/admin/group/GroupRateMultipliersModal.vue`
  - `frontend/src/views/admin/UsageView.vue`
  - `frontend/src/views/admin/GroupsView.vue`
