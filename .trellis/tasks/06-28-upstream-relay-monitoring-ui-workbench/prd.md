# 上游倍率监控 UI 决策台调整

## Goal

将管理员“上游倍率监控”页面从资源 CRUD 页面调整为运维决策台。首屏应优先呈现运行概览和候选映射主表，低频配置动作收进弹层，Priority 应用前展示风险摘要。

## What I Already Know

- 本次只改前端 UI，不新增后端接口，不改变现有 API wire shape。
- 当前主要页面是 `frontend/src/views/admin/UpstreamRelayGroupMonitoringView.vue`。
- 现有 API 类型位于 `frontend/src/api/admin/upstreamRelayGroupMonitors.ts`，本次不修改请求/响应结构。
- 当前分支是 `feature/group-visible-rate-multiplier`，任务 base branch 为 `custom/main`。

## Requirements

- 顶部加入标题、最近刷新/同步信息、刷新和生成建议操作。
- 新增运行概览卡：可用连接器、需重新登录、启用候选、探测失败、待应用建议、最近同步时间。
- 默认主区域改为候选映射，强化倍率来源、健康状态、Priority 建议差异。
- 连接器和候选表单从常驻卡片改为弹层/抽屉。
- 连接器列表新增查看快照入口，快照在弹层中展示。
- Priority 应用确认弹窗增加风险摘要和 run 创建时间。

## Acceptance Criteria

- [ ] 页面加载后首屏能看到概览、候选主表和主要操作。
- [ ] 新建/编辑连接器、新建/编辑候选在弹层中完成，保存失败时输入不丢。
- [ ] 候选探测只让当前行进入 loading，不锁死整页。
- [ ] 连接器同步后可从连接器行查看快照。
- [ ] 生成建议后，候选表能体现命中的 Priority 建议变化。
- [ ] 应用建议弹窗显示风险摘要和明细，确认后刷新 run 状态。
- [ ] 前端类型检查和构建通过。

## Follow-up TODO

按“先减少重复录入，再提升批量处理效率，最后强化建议确认闭环”的顺序推进。

### Step 0: 复用账号创建候选

- [x] 在候选行增加“复用账号”入口。
- [x] 打开新建候选弹层时复用连接器、账号、探测模型、协议和启用状态。
- [x] 清空上游 Group ID、目标分组和 notes，保存时走创建候选流程。
- [x] 提交本步骤前再次运行 `pnpm --dir frontend typecheck`。

### Step 1: 从快照创建候选

- [x] 在连接器快照弹层的快照行增加“创建候选”入口。
- [x] 自动带入当前连接器和上游 Group ID。
- [x] 如果快照包含名称/平台信息，在表单旁展示为只读上下文，帮助选择本地账号和目标分组。
- [x] 保存仍使用现有创建候选接口，不新增后端 API。

### Step 2: 保存并继续添加

- [x] 候选弹层新增次级按钮“保存并继续添加”。
- [x] 保存成功后保留连接器、账号、探测模型、协议和启用状态。
- [x] 清空上游 Group ID、目标分组和 notes，保持弹层打开，方便连续录入多个分组。
- [x] 普通“创建候选/更新候选”按钮维持现有关闭弹层行为。

### Step 3: 重复候选提示

- [x] 在候选表单内检测当前已加载候选中是否存在相同连接器、账号和上游 Group ID。
- [x] 命中重复时展示提示，说明已有候选和目标分组。
- [x] 第一版只做前端提示，不阻断提交，避免和后端校验策略冲突。

### Step 4: 表单联动默认值

- [ ] 新建候选时，选择本地账号后尽量复用该账号最近一次候选的探测模型和协议。
- [ ] 目标分组列表优先展示与账号平台一致的分组。
- [ ] 保留完整分组选项，避免平台数据缺失时无法选择。

### Step 5: 候选快速筛选

- [ ] 候选主表增加筛选：连接器、账号、健康状态、倍率来源、是否有待应用建议。
- [ ] 概览卡点击后能切到候选视图并应用对应筛选。
- [ ] 空态文案区分“没有候选”和“筛选后无结果”。

### Step 6: 批量探测

- [ ] 在候选主表增加“批量探测当前筛选结果”入口。
- [ ] 至少支持同账号/同连接器场景的批量触发。
- [ ] 每行保持独立 loading 或批量运行摘要，避免误以为整页卡死。

### Step 7: Priority 建议反查候选

- [ ] 应用建议弹窗中的建议行增加“查看候选”入口。
- [ ] 能跳回候选视图并定位或筛选到对应候选。
- [ ] 低置信建议旁提供“先探测”行动入口，减少关闭弹窗后手动查找。

## Out of Scope

- 不做图表、趋势曲线、独立路由或可配置 dashboard。
- 不新增后端统计接口。
- 不修改现有 API 请求/响应类型。

## Technical Notes

- 概览统计基于当前已加载数据，文案避免暗示绝对全量。
- 如果没有现成抽屉组件，使用固定定位弹层实现，不引入新 UI 依赖。
- 删除连接器和候选继续使用确认弹窗，但文案需要说明影响范围。
