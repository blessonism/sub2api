# 实施计划

1. 设置任务分支/基线和下游上下文，新增 192 号迁移、Ent schema、领域类型及仓储实现。
2. 实现收件人计算、Markdown 邮件快照、唯一任务创建、失败重试、删除保护和数据库 Worker。
3. 接入 Wire、服务生命周期、管理员 Handler 与四个 API 路由。
4. 扩展前端类型/API，在公告管理页增加发送确认、状态轮询、投递明细与失败重试。
5. 补齐后端、迁移、API、前端组件与 i18n 测试，运行 60 秒以内的定向检查和完整类型检查。

## 回滚点

- 迁移只新增表；回滚应用代码时保留表与数据。
- Worker 不修改公告或用户实体，只更新新增群发表；发生问题可停止 Worker 并保留 pending/failed 状态。
