# 实施计划

1. **激活分支与任务**
   - 从 `custom/main` 创建并切换 `fix/usage-calibration-revert`。
   - 执行 `task.py start`，确认任务进入 `in_progress`。

2. **后端数据与撤销 API**
   - 增加撤销字段迁移、服务 DTO/接口、仓储事务和统计过滤。
   - 增加 handler 与管理员路由，保持现有鉴权与错误响应风格。
   - 移除使用记录/仪表盘对余额校准实际成本的加总。

3. **前端历史撤销交互**
   - 扩展 API 类型并新增撤销请求。
   - 在 `user/UsageView.vue` 增加撤销按钮、确认、状态展示、刷新与错误处理。
   - 增加中英文文案和行为测试。

4. **验证与收尾**
   - 运行后端仓储/服务/仪表盘定向测试与前端 UsageView 测试、类型检查。
   - 执行 Trellis check，复核差异与未提交外部改动。
   - 提交前单独向用户请求 `git commit` 明确确认；本任务不自动提交或推送。
